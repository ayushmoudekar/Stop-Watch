Project overview :- 
Stop Watch

A simple Stop Watch application that utilizes JavaScript's setTimeout and setInterval functions to measure and display elapsed time.

1. Control the stopwatch with buttons to start, stop, and reset the timer.
2. Shows elapsed time in a user-friendly format (hours, minutes, seconds, milliseconds).
3. Accessible and usable on different screen sizes and devices.

Technologies Used: HTML , CSS & JAVASCRIPT
